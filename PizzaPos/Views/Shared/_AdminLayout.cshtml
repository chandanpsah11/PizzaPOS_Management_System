<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>@ViewBag.Title - Pizza POS Management System</title>
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.4.1/css/bootstrap.min.css" />
    <link rel="stylesheet" href="~/Content/Site.css" />
</head>
<body>
    <div class="navbar navbar-inverse navbar-fixed-top">
        <div class="container-fluid">
            <div class="navbar-header">
                <button type="button" class="navbar-toggle"
                        data-toggle="collapse" data-target="#adminMenu">
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                </button>
                <a href="@Url.Action("Index", "AdminInventory")" class="navbar-brand">
                    <span class="glyphicon glyphicon-fire"></span>
                    Pizza POS
                </a>
            </div>

            <div id="adminMenu" class="navbar-collapse collapse">
                <ul class="nav navbar-nav">
                    <li>@Html.ActionLink("Inventory", "GetAllInventory", "AdminInventory")</li>
                    <li>@Html.ActionLink("Pizza Sales", "GetPizzas", "AdminSales")</li>
                    <li>@Html.ActionLink("Orders", "GetOrders", "Orders")</li>
                </ul>

                <ul class="nav navbar-nav navbar-right">
                    <li class="navbar-text">
                        <span class="glyphicon glyphicon-lock"
                              style="color:#d9534f; margin-right:4px"></span>
                        <span class="label label-danger"
                              style="font-size:11px; vertical-align:middle">
                            ADMIN
                        </span>
                    </li>
                    <li class="navbar-text">
                        <span class="glyphicon glyphicon-user"
                              style="margin-right:4px"></span>
                        <strong style="color:#fff">
                            @(Session["UserName"] ?? User.Identity.Name)
                        </strong>
                    </li>
                    <li>
                        @Html.ActionLink("Logout", "Logout", "Account",
                            null, new
                            {
                                @class = "btn btn-danger navbar-btn",
                                style  = "margin-left:8px"
                            })
                    </li>
                </ul>

            </div>
        </div>
    </div>

    <div class="container-fluid body-content" style="margin-top:70px">
        @RenderBody()
        <hr />
        <footer>
            <p class="text-muted">
                &copy; @DateTime.Now.Year - Pizza POS Management System
                &nbsp;|&nbsp;
                <small>
                    Logged in as
                    <strong>@(Session["UserName"] ?? User.Identity.Name)</strong>
                    &nbsp;&mdash;&nbsp;
                    <span class="text-danger">Administrator</span>
                </small>
            </p>
        </footer>
    </div>
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.7.1/jquery.min.js"></script>
    <script src="https://maxcdn.bootstrapcdn.com/bootstrap/3.4.1/js/bootstrap.min.js"></script>
    <script src="~/Scripts/jquery.unobtrusive-ajax.min.js"></script>
    @RenderSection("scripts", required: false)
</body>
</html>